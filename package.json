{
  "name": "vue-spa",
  "scripts": {
    "start": "concurrently 'cd app1 && yarn start' 'cd navbar && yarn start' 'serve -s -l 5000'",
    "install": "yarn install && cd app1 && yarn install && cd navbar && yarn install"
  },
  "devDependencies": {
    "serve": "^11.1.0"
  },
  "dependencies": {
    "concurrently": "^5.0.0"
  }
}
